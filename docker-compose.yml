services:
  cyfr:
    container_name: cyfr
    image: ghcr.io/cyfrworks/cyfr:latest
    ports:
      - "4000:4000"
      - "4001:4001"
    volumes:
      - ./data:/app/data
      - ./components:/app/components
    env_file:
      - .env

  # ════════════════════════════════════════════
  # Caddy — Production Reverse Proxy (profile: caddy)
  # Activate with COMPOSE_PROFILES=caddy in .env
  # ════════════════════════════════════════════

  caddy:
    profiles: [caddy]
    build:
      context: .
      dockerfile: Dockerfile.caddy
      args:
        - EXPO_PUBLIC_CYFR_URL=/mcp
        - EXPO_PUBLIC_CYFR_PUBLIC_KEY=${EXPO_PUBLIC_CYFR_PUBLIC_KEY}
        - EXPO_PUBLIC_SUPABASE_URL=${EXPO_PUBLIC_SUPABASE_URL}
        - EXPO_PUBLIC_SUPABASE_KEY=${EXPO_PUBLIC_SUPABASE_KEY}
    ports:
      - "80:80"
      - "443:443"
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN:-localhost}
    volumes:
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - cyfr

volumes:
  caddy-data:
  caddy-config:
