# CYFR Server
CYFR_SECRET_KEY_BASE=generate-with-cyfr-init
CYFR_PORT=4000
CYFR_HOST=localhost
CYFR_GITHUB_CLIENT_ID=Ov23lib66tiIwXkgUpwm

# Frontend (Vite exposes VITE_ prefixed vars to the browser)
VITE_CYFR_URL=/cyfr
VITE_CYFR_PUBLIC_KEY=cyfr_pk_...

# Optional: Restrict to specific users
CYFR_ALLOWED_USER=cyfr-user-oauth-email

# === Supabase ===
# Option A: Supabase Cloud (default — no profile needed)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=sb_publishable_...

# Option B: Self-hosted (run ./scripts/setup-supabase.sh first)
# VITE_SUPABASE_URL=http://localhost:8000
# VITE_SUPABASE_KEY=<generated-anon-key>

# === Docker Compose Profiles ===
# Uncomment/set as needed:
# COMPOSE_PROFILES=supabase        # Self-hosted Supabase only
# COMPOSE_PROFILES=caddy           # Production Caddy only (cloud Supabase)
# COMPOSE_PROFILES=supabase,caddy  # Both (fully self-contained production)

# === Production (Caddy) ===
# SITE_DOMAIN=yourdomain.com       # Used by Caddy for auto TLS
